# TODO

- [done] Переписать `src/invest_toolkit/io/sl_scrapper.py` для инкрементального обновления локальных CSV SmartLab по новым годам из HTML-таблиц страниц компаний.
- [done] Скрапер SmartLab больше не опирается на кнопку скачивания CSV: тикеры берутся из уже сохранённых файлов в целевой папке, а в локальные CSV добавляются только отсутствующие годовые колонки.
- [done] Перевести `src/invest_toolkit/io/sl_scrapper.py` на обновление по семействам тикеров (`BASE`/`BASEP`) и добавить выборочный запуск по списку тикеров.
- [done] Для семейства тикеров использовать одну каноническую страницу SmartLab с fallback внутри семейства, например `SBER/SBERP -> SBER`, `WTCM/WTCMP -> WTCMP`.
- [done] Добавить инициализацию логгера при прямом запуске `src/invest_toolkit/io/sl_scrapper.py` через блок `if __name__ == "__main__":`.
- [done] По итогам прогона семейная логика для префов работает, массовые `404` по тикерам с суффиксом `P` устранены.
- [done] Сетевые ошибки вида `ConnectTimeout` при обращении к SmartLab (например по `NKNC`) признаны временными внешними сбоями; отдельные ретраи и спец-обработка сейчас не внедряются.
